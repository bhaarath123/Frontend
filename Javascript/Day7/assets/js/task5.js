// 1. Added the 'return' keyword here
const addnumbers = (a, b) => {
    return a + b; 
}

// 2. The function now successfully returns 65 into this variable
const totalSum = addnumbers(25, 40);

// 3. Removed the accidental '+' symbol
console.log("This is sum:", totalSum); 
