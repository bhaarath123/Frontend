let age = 22;
let hasId = true;

// 1. Age is 18 or above AND has ID
console.log(age >= 18 && hasId);

// 2. Age is below 18 OR has ID
console.log(age < 18 || hasId);

// 3. NOT hasId
console.log(!hasId);
